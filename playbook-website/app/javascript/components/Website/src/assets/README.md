# Assets Folder

This folder is for small React-local assets that belong to the website app.
Currently it contains `CodeSandboxIcon.tsx`.

Most shared images and SVGs used by the website are imported from the broader
`app/javascript/images` alias instead of this folder. Add assets here only when
they are specific to the React website code and are not part of the shared image
set.
