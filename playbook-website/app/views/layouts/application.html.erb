<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/head'%>
    <%= vite_client_tag %>
    <%= vite_javascript_tag(*@application_js) %>
  </head>
  <body class="<%= @body_class %> <%= controller_name %> <%= action_name%>">
    <div class="pb--page--dark-mode-toggle-mobile">
    </div>
    <%# Flex chain for main-column scroll — see `website_new.scss` (`.pb--website-layout-root`) %>
    <div class="pb--website-layout-root">
      <%= react_component("Website", {}, { class: "pb--website-react-host" }) %>
    </div>
  </body>

</html>
