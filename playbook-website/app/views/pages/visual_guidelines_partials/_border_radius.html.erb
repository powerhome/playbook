<%= render partial: "pages/visual_guidelines_partials/partial_title",
  locals: {
    title: "Border Radius",
    title_size: 1,
    description: "We have very specific settings for border radius to keep the interface
                  looking consistent and clean. If you ever need to access these to build
                  new things here are examples for how to do that.",
    global_props: false
  }
%>
<%= pb_rails("title", props: {
  size: 4,
  tag: "h3",
  text: "Tokens"
}) %>
<%= pb_rails("body", props: {
  margin_bottom: "lg",
  margin_top: "xs",
  tag: "p"
}) do %>
  Make up your own styles using Playbook tokens to keep your site consistent.
<% end %>
<%= pb_rails("card", props: {
  margin_bottom: "none",
  padding: "none",
  shadow: "deep"
}) do %>
  <%= render "pages/visual_guidelines_partials/pb_doc_border_radius" %>
  <%= render partial: "pages/visual_guidelines_partials/partial_code_example",
    locals: {
      example: "app/views/pages/code_snippets/border_radius_tokens.txt"
    }
  %>
<% end %>
