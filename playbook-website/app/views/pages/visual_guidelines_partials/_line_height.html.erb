<%= render partial: "pages/visual_guidelines_partials/partial_title",
  locals: {
    title: "Line Height",
    title_size: 1,
    description: "Odds are you might want to build or expand upon the text patterns we have provided.
                  If so below is a good reference of the tokens that are available for you and your
                  typography needs.",
    global_props: false
  }
%>
<%= pb_rails("title", props: {
  size: 4,
  tag: "h3",
  text: "Tokens"
}) %>
<%= pb_rails("body", props: {
  margin_bottom: "lg",
  margin_top: "xs",
  tag: "p"
}) do %>
  Make up your own styles using Playbook tokens to keep your site consistent.
<% end %>
<%= pb_rails("card", props: {
  margin_bottom: "none",
  padding: "none",
  shadow: "deep"
}) do %>
  <%= render "pages/visual_guidelines_partials/pb_doc_line_height"%>
  <%= render partial: "pages/visual_guidelines_partials/partial_code_example",
    locals: {
      example: "app/views/pages/code_snippets/line_height_jsx.txt"
    }
  %>
<% end %>
