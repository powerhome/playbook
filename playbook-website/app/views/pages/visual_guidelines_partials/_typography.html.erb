<%= pb_rails("title", props: { text: "Typography", size: 1, margin_top: "xl", padding_top: "xl" }) %>
<%= pb_rails("body", props: { tag: "p", margin_top: "xs", margin_bottom: "lg" }) do %>
  Odds are, you might want to build or expand upon the text patterns we have 
  provided. Below are the tokens that are available for you and your 
  typography needs.
<% end %>
<%= pb_rails("title", props: { text: "Tokens", tag: "h3", size: 4 }) %>
<%= pb_rails("body", props: { tag: "p", margin_bottom: "lg", margin_top: "xs" }) do %>
  Make up your own styles using Playbook tokens to keep your site consistent.
<% end %>
<%= pb_rails("card", props: { shadow: "deep", margin_bottom: "none", padding: "none" }) do %>
  <%= render "pages/visual_guidelines_partials/typography_headings"%>
  <%= pb_rails("section_separator") %>
  <%= render "pages/visual_guidelines_partials/typography_size"%>
  <%= pb_rails("section_separator") %>
  <%= render "pages/visual_guidelines_partials/typography_spacing"%>
  <%= pb_rails("background", props: { id: "code-wrapper", classname: "border-bottom", background_color: "dark", padding: "none" }) do %>
    <%= pb_rails("caption", props: { text: "EXAMPLE IN USE", padding_top: "lg", margin_x: "xl", classname: "dark" }) %>
    <div class="pb--codeCopy">
      <pre class="highlight pt_sm" style="margin: 0px"><%= raw rouge(@typography_token_example, "react") %></pre>
    </div>
  <% end %>        
<% end %>
