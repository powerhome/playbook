import React from 'react'
import Card from '../_card.jsx'

const CardLight = (props) => {
  return (
    <div>
      <Card maxWidth="sm">{'Card content'}</Card>
    </div>

     <div>
      <Card maxWidth="md">{'Card content'}</Card>
    </div>

     <div>
      <Card maxWidth="lg">{'Card content'}</Card>
    </div>

     <div>
      <Card maxWidth="xl">{'Card content'}</Card>
    </div>
  )
}

export default CardLight
