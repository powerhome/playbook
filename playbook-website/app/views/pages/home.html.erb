<div class="landing-page">
<%= react_component("HomepageHero") %>
  <%= render(LgHpSocialProofComponent.new) %>
  <%= render(MdHpSocialProofComponent.new) %>
  <%= render(SmHpSocialProofComponent.new) %>
  <%= render(HpSolutionsComponent.new)%>


  <%= pb_rails("background", props: { background_color: "white" }) do %>
    <%= pb_rails("flex", props: { orientation: "column", align: "center", padding_top: "lg" }) do %>
      <%= pb_rails("title", props: { color: "link", text: "NEWEST FROM PLAYBOOK", tag: "h4", size: 4, padding_bottom: "sm" }) %>
      <%= pb_rails("title", props: { text: "Playbook is always shipping.", tag: "h2", size: 2, text_align: "center", padding_bottom: "xs" }) %>
      <%= pb_rails("caption", props: { size: "md", text: "Move Fast. Follow Momentum.", padding_bottom: "sm" }) %>
    <% end %>

    <% @structured_data.each.with_index do |post, index| %>
      <%= render(PreviewCardComponent.new(post: post, index: index)) %>
    <% end %>
  <% end %>
</div>
