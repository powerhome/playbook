inherit_from:
  - ../.rubocop.yml

inherit_mode:
  merge:
    - Exclude

require:
  - rubocop-performance

AllCops:
  Exclude:
    - "config/**/*"
    - "bin/*"
    - "node_modules/**/*"
    - "vendor/**/*"
