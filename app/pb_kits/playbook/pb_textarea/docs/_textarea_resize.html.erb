<%= pb_rails("textarea",
    props: {
        label: "vertical",
        input_options: {
            placeholder: "Resize Vertical",
            resize: "vertical"
        }
    }
) %>

<br>

<%= pb_rails("textarea",
    props: {
        label: "both",
        input_options: {
            placeholder: "Resize Both",
            resize: "both"
        }
    }
) %>

<br>

<%= pb_rails("textarea",
    props: {
        label: "horizontal",
        input_options: {
            placeholder: "Resize Horizontal",
            resize: "horizontal"
        }
    }
) %>
