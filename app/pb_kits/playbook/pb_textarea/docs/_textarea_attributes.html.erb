<%= pb_rails("textarea",
    props: {
        disabled: true,
        label: "Disabled",
        name: "disabled",
    }
) %>

<br>

<%= pb_rails("textarea",
    props: {
        label: "Min & Max Character Length",
        maxlength: 10,
        minlength: 5,
        name: "minmax",
        placeholder: "This has a min-length of 5 and a max-length of 10 characters.",
    }
) %>

<br>

<%= pb_rails("textarea",
    props: {
        label: "Required",
        name: "required",
        placeholder: "This textarea is required.",
        required: true
    }
) %>
