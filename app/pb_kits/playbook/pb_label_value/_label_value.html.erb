<%= content_tag(:div,
    id: object.id,
    data: object.data,
    class: object.classname) do %>
  <%= pb_rails("caption", props: { text: "#{object.label}" }) if object.label? %>
  <%= pb_rails("body", props: { text: "#{object.value}" }) %>
<% end %>
