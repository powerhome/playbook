
  <%= pb_rails("date", props: {
    date: DateTime.now,
    icon: true,
    day_of_week: true
  }) %>

  <br><br>

  <%= pb_rails("date", props: {
    date: DateTime.now,
    icon: true,
    day_of_week: true,
    alignment: "center"
  }) %>

  <br><br>

  <%= pb_rails("date", props: {
    date: DateTime.now,
    icon: true,
    day_of_week: true,
    alignment: "right"
  }) %>
  
<br><br>

  <%= pb_rails("date", props: {
  date: Date.today,
  size: "lg",
  day_of_week: true
}) %>

<%= pb_rails("date", props: {
  date: Date.today,
  size: "lg",
  day_of_week: true,
  alignment: "center"
}) %>

<%= pb_rails("date", props: {
  date: Date.today,
  size: "lg",
  day_of_week: true,
  alignment: "right"
}) %>



