<%= pb_rails("date", props: {
  date: Date.today,
  size: "lg"
}) %>

<br>

<%= pb_rails("date", props: {
  date: DateTime.now
}) %>

<br>

<%= pb_rails("date", props: {
  date: DateTime.now,
  timezone: "Asia/Tokyo"
}) %>

<br>

<%= pb_rails("date", props: {
  date: Date.new(2010, 11, 12),
  size: "xs"
}) %>
