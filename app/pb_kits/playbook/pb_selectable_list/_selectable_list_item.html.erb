<%= content_tag(:li,
    aria: object.aria,
    class: object.classname,
    data: object.data,
    id: object.id) do %>
    <% puts object.variant %>
  <% if object.variant == "radio"%>
    <%= pb_rails("radio", props: {text: object.text} ) %>
    <% if object.children.present? %>
      <%= capture(&object.children) %>
    <% end %>
  <% elsif object.variant == "checkbox" %>
    <%= pb_rails("checkbox", props: {text: object.text} ) %>
    <% if object.children.present? %>
      <%= capture(&object.children) %>
    <% end %>
  <% else %>
    <h1>BAD</h1>
  <% end %>
<% end %>
