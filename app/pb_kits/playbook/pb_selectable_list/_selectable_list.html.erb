<%= content_tag(:div,
    aria: object.aria,
    class: object.classname,
    data: object.data,
    id: object.id) do %>
      <%= pb_rails("list") do %>
        <%# object.children.each { |child| child.props[:variant] = object.variant }%>
        <% binding.pry %>
        <%= capture(object.variant, &object.children) %>
      <% end %>
<% end %>


