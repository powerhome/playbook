<%= content_tag(:div,
    aria: object.aria,
    class: object.classname,
    data: object.data,
    id: object.id) do %>
      <%= !object.children.nil? ? capture(&object.children) : object.text %>
<% end %>
