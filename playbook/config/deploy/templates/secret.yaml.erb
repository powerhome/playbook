<% require "base64" %>
apiVersion: v1
kind: Secret
metadata:
  name: playbook
  labels:
    app: playbook
type: Opaque
data:
  secret-key-base: <%= Base64.encode64(appConfig.fetch("secretKeyBase")).gsub("\n", "").strip %>
