apiVersion: v1
kind: Service
metadata:
  name: playbook
  labels:
    app: playbook
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: playbook
  selector:
    app: playbook
