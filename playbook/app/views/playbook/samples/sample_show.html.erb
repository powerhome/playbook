<%= pb_rails("background", props: { background_color: "light", padding: "xl" }) do %>

  <%= pb_rails("title", props: { 
    margin_bottom: "md",
    text: "Samples  /  #{get_category(@sample).titleize}",
    tag: "h4",
    size: 4
  }) %> 
  <%= pb_rails("title", props: { 
    margin_bottom: "xl",
    text: @sample.titleize,
    tag: "h2",
    size: 2
  }) %>

  <%= pb_rails("card", props: { id: "sample-card", padding: "none", shadow: "deeper" }) do %>
    <%= pb_rails("card/card_header", props: { padding: "none", header_color: "white" }) do %>

      <%= pb_rails("flex", props: { spacing: "between", vertical: "center" }) do %>
  
        <%= pb_rails("flex/flex_item", props: { margin_left: "sm" }) do %>
          <%= pb_rails("nav", props: { orientation: "horizontal" }) do %>
            <%= pb_rails("nav/item", props: { text: "Rails", link: "#{sample_show_rails_path(@sample)}", active: action_name == 'sample_show_rails' }) %>
            <%= pb_rails("nav/item", props: { text: "React", link: "#{sample_show_reacts_path(@sample)}", active: action_name == 'sample_show_react' }) %>
          <% end %>
        <% end %>

        <%= pb_rails("flex/flex_item", props: { margin_right: "sm" }) do %>
          <%= pb_rails("flex") do %>

            <%= pb_rails("body", props: { color: "light", id: 'kits-used' }) do %>
                Kits Used
                <%= pb_rails("icon", props: { icon: "angle-down"}) %>
            <% end %>
            <%= pb_rails("popover", props: { max_width: "300px", trigger_element_id: "kits-used", tooltip_id: "kits-used-tooltip", position: 'bottom', padding: "sm" }) do %>
              <%= pb_rails("flex", props: { spacing: "around", wrap: true }) do %>
                <% kits_used(@sample).each do |kit| %>
                  <a href=<%="/kits/#{kit[0]}"%>>
                    <%= pb_rails("pill", props: {
                      text: kit[0].titleize,
                      variant: "primary",
                    }) %>
                  </a>
                <% end %>
              <% end %>
            <% end %>

            <%= pb_rails("icon_value", props: {
              classname: "toggle-button-js",
              icon: "code",
              margin_left: "sm",
              text: "Code"
            }) %>
            <%= pb_rails("icon_value", props: {
              id: "fullscreen-toggle",
              icon: "expand",
              margin_left: "sm",
              text: "Fullscreen"
            }) %>
          <% end %>
          <%= pb_rails("circle_icon_button", props:{ classname: "compress", variant: "link", icon: "compress"}) %>
          
          <% end %>
        <% end %>
      
    <% end %>


    <%= pb_rails("card/card_body", props: { classname: "bg_light", padding: "none" }) do %>
      <%= render_sample_ui(@sample, @type) %>
    <% end %>
  <% end %>

<% end %>

<div class="pb--kit-show">
    <div class="pb--doc light_ui">
        <div class="pb--codeCopy close" >
            <a class="pb--close-toggle copy-clipboard" href="#">Copy to Clipboard</a>
            <div class="hiddenCodeforCopy"><%= get_raw_code(@sample, @type) %></div>
            <pre class="highlight"><%= get_sample_code_content(@sample, @type) %></pre>
        </div>
    </div>
</div>