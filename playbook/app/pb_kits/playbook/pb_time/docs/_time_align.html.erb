<%= pb_rails("time", props: {
  time: DateTime.now,
  align: "left"
}) %>

<br>

<%= pb_rails("time", props: {
  time: Time.now,
  align: "center"
}) %>

<br>

<%= pb_rails("time", props: {
  time: Time.now,
  align: "right"
}) %>
