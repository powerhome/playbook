```swift
VStack(alignment: .leading, spacing: Spacing.small) {
      PBTime(showTimeZone: true, 
             variant: .iconTimeZone)
      PBTime(showTimeZone: true, 
             variant: .iconTimeZone,
             isLowercase: true,
             isBold: true,
             unstyled: .body
      )
    }
```
