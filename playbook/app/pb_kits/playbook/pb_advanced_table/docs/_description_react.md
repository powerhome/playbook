Advanced Table is available through a separate entrypoint so TanStack stays out of the main `playbook-ui` bundle until this kit is used. TanStack is still bundled with that entrypoint, no additional installation is required.

**NOTE**: Import Advanced Table using:
```javascript
import { AdvancedTable } from 'playbook-ui/advanced-table'
```
