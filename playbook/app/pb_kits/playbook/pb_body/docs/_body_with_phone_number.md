Use the `formatPhoneNumber` utility function (React) or `format_phone_number` helper (Rails) to display phone numbers with the Body kit. This allows you to use any typography style and color while maintaining consistent phone number formatting.

