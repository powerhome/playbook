import React from 'react'
import { Legend } from '../..'

const LegendStatusColors = (props) => (
  <div>
    <Legend
        color="success"
        text="Windows"
        {...props}
    />
  </div>
)

export default LegendStatusColors
