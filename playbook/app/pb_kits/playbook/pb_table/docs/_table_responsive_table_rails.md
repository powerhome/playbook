Use `responsive: "none"` to keep the default table layout at all breakpoints.

Use `responsive: "scroll"` to keep headers visible and enable horizontal scrolling on smaller screens.

Use `responsive: "collapse"` (default) with `collapse` set to `"sm"`, `"md"`, or `"lg"` to stack cells with column labels at that breakpoint.
