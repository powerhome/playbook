<% if object.tag == "table" %>
    <%= content_tag(:td,
      aria: object.aria,
      class: object.classname,
      data: object.data,
      id: object.id) do %>
      <%= content.presence || object.text %>
    <% end %>
<% else %>
    <%= content_tag(:div,
      aria: object.aria,
      class: object.classname,
      data: object.data,
      id: object.id) do %>
      <%= content.presence || object.text %>
    <% end %>
<% end %>