Use the Contact kit with `iconEnabled={false}` and `unstyled={true}` to display phone numbers with full typography control. When `unstyled={true}`, the Contact kit renders just the formatted text without a Body wrapper, allowing you to wrap it in your own Body kit to control the color and styling.

