<%= content_tag(:div,
    aria: object.aria,
    class: object.classname,
    data: object.data,
    id: object.id) do %>
     <%= will_paginate object.page_data, renderer: Playbook::Pagination::LinkRenderer %>
<% end %>
