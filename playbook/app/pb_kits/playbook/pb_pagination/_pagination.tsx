import React, { useState } from "react";

// import Button from '../pb_body/_body'

type PaginationProps = {
  defaultPage?: number;
  pageRange?: number;
  totalPages?: number;
};

const Pagination = (props: PaginationProps): React.ReactElement => {

  return (
    <>
    {"hi"}

    </>
  );
};

export default Pagination;
