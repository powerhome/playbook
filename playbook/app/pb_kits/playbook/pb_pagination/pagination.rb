# frozen_string_literal: true

module Playbook
  module PbPagination
    class Pagination < ::Playbook::KitBase
      # require "json"
      # file = File.read("/.user.json")
      # data = JSON.parse file

      # @user_data = data.paginate(page: params[:page], per_page: 20)
    end
  end
end
