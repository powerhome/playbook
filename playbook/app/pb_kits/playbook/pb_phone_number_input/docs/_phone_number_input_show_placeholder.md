The `showPlaceholder`/`show_placeholder` prop enables the country-specific example placeholder in the phone input.

By default, the Phone Number Input does not show a placeholder. When enabled, the component uses the library's default placeholder behavior.
